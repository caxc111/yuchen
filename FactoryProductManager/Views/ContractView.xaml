<UserControl x:Class="FactoryProductManager.Views.ContractView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="10,0,20,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Image Source="pack://application:,,,/Resources/Icons/icon_contract.png" Width="32" Height="32" Margin="0,0,10,0"/>
            <TextBlock Text="合同管理" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Border Style="{StaticResource WarmPanelBorderStyle}" Padding="10"/>
            <Border Background="Transparent" BorderThickness="0" CornerRadius="10" Padding="10">
                <StackPanel Orientation="Horizontal">
                    <Grid Width="220" Height="35">
                        <TextBox Width="220" Style="{StaticResource WarmSearchTextBoxStyle}"
                                 Padding="10,0" FontSize="14" Foreground="{StaticResource PrimaryTextBrush}"
                                 materialDesign:HintAssist.Hint="搜索合同号..."/>
                    </Grid>
                    <Border Width="80" Height="35" Margin="20,0,0,0" Style="{StaticResource WarmActionBorderStyle}">
                        <Button Content="搜索" Style="{StaticResource WarmActionButtonStyle}" Foreground="{StaticResource EditAccentBrush}"/>
                    </Border>
                    <Border Width="100" Height="35" Margin="20,0,0,0" Style="{StaticResource WarmActionBorderStyle}">
                        <Button Content="新增合同" Style="{StaticResource WarmActionButtonStyle}" Foreground="{StaticResource SuccessAccentBrush}"/>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Border Style="{StaticResource WarmPanelBorderStyle}" Padding="10">
                <DataGrid Style="{StaticResource GlobalBaseDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="合同号" Binding="{Binding ContractNo}" Width="160" HeaderStyle="{StaticResource GlobalDataGridHeaderStyle}" ElementStyle="{StaticResource GlobalDataGridTextElementStyle}"/>
                        <DataGridTextColumn Header="客户名称" Binding="{Binding CustomerName}" Width="*" HeaderStyle="{StaticResource GlobalDataGridHeaderStyle}" ElementStyle="{StaticResource GlobalDataGridTextElementStyle}"/>
                        <DataGridTextColumn Header="总金额" Binding="{Binding TotalAmount}" Width="140" HeaderStyle="{StaticResource GlobalDataGridHeaderStyle}" ElementStyle="{StaticResource GlobalDataGridTextElementStyle}"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100" HeaderStyle="{StaticResource GlobalDataGridHeaderStyle}" ElementStyle="{StaticResource GlobalDataGridTextElementStyle}"/>
                        <DataGridTextColumn Header="签订日期" Binding="{Binding SignDate}" Width="140" HeaderStyle="{StaticResource GlobalDataGridHeaderStyle}" ElementStyle="{StaticResource GlobalDataGridTextElementStyle}"/>
                        <DataGridTextColumn Header="交货日期" Binding="{Binding DeliveryDate}" Width="140" HeaderStyle="{StaticResource GlobalDataGridHeaderStyle}" ElementStyle="{StaticResource GlobalDataGridTextElementStyle}"/>
                        <DataGridTemplateColumn Header="操作" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button ToolTip="查看明细" Margin="0,0,5,0" Style="{StaticResource WarmIconButtonStyle}">
                                            <Image Source="/Resources/Icons/icon_expand.png" Width="14" Height="14" Stretch="Uniform"/>
                                        </Button>
                                        <Button ToolTip="编辑" Margin="0,0,5,0" Style="{StaticResource WarmIconButtonStyle}">
                                            <Image Source="/Resources/Icons/icon_edit.png" Width="14" Height="14" Stretch="Uniform"/>
                                        </Button>
                                        <Button ToolTip="删除" Style="{StaticResource WarmIconButtonStyle}">
                                            <Image Source="/Resources/Icons/icon_delete.png" Width="14" Height="14" Stretch="Uniform"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
