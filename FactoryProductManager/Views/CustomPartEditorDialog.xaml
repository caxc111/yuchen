<Window x:Class="FactoryProductManager.Views.CustomPartEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FactoryProductManager.Views"
        xmlns:app="clr-namespace:FactoryProductManager"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="自定义部件" Height="500" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Background="{StaticResource WarmSurfaceBrush}"
        WindowStyle="None"
        AllowsTransparency="True"
        app:WindowDragBehavior.Enable="True"
    >
    <Border Style="{StaticResource UnifiedDialogBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="自定义部件配置" FontSize="16" FontWeight="Bold"
                           Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Style="{StaticResource WindowButtonStyle}" Click="MinimizeButton_Click" ToolTip="最小化">
                        <materialDesign:PackIcon Kind="WindowMinimize" Width="14" Height="14"/>
                    </Button>
                    <Button x:Name="MaximizeButton" Style="{StaticResource WindowButtonStyle}" Click="MaximizeButton_Click" ToolTip="最大化">
                        <materialDesign:PackIcon Kind="WindowMaximize" Width="14" Height="14"/>
                    </Button>
                    <Button Style="{StaticResource WindowCloseButtonStyle}" Click="CloseButton_Click" ToolTip="关闭">
                        <materialDesign:PackIcon Kind="Close" Width="14" Height="14"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="部件名称" FontSize="13"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource PrimaryTextBrush}"/>
                <TextBox x:Name="PartNameTextBox" Grid.Column="1"
                         FontSize="14" Height="32" Padding="8,0"
                         VerticalContentAlignment="Center"
                         Background="{StaticResource WarmInputBrush}"
                         BorderBrush="{StaticResource InputBorderBrush}"
                         Foreground="{StaticResource PrimaryTextBrush}"/>
            </Grid>

            <Border Grid.Row="2" Background="{StaticResource WarmHeaderBrush}"
                    CornerRadius="8" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="部品选择（多选）" FontSize="13" FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource PrimaryTextBrush}"
                                   Margin="0,0,12,0"/>
                        <Button Content="+ 自定义部品" Click="AddCustomComponent_Click"
                                Style="{StaticResource AddMaterialButtonStyle}"
                                Width="110" Height="26" FontSize="12"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ComponentListPanel"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="取消" Click="CancelButton_Click"
                        Style="{StaticResource AddMaterialButtonStyle}"
                        Width="80" Height="32" Margin="0,0,10,0"/>
                <Button Content="保存" Click="OkButton_Click"
                        Style="{StaticResource AddMaterialButtonStyle}"
                        Width="80" Height="32"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
