<UserControl x:Class="FactoryProductManager.Views.BOMView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Image Source="pack://application:,,,/Resources/Icons/icon_bom.png" Width="32" Height="32" Margin="0,0,10,0"/>
            <TextBlock Text="BOM管理" FontSize="24" FontWeight="Bold"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <ComboBox Width="150" materialDesign:HintAssist.Hint="选择成品"/>
            <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="添加组件" Margin="10,0"/>
            <Button Style="{StaticResource MaterialDesignFloatingActionButton}" Margin="10,0,0,0">
                <materialDesign:PackIcon Kind="Plus"/>
            </Button>
        </StackPanel>

        <DataGrid Grid.Row="2" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="成品名称" Binding="{Binding ProductName}"/>
                <DataGridTextColumn Header="组件编码" Binding="{Binding ComponentCode}"/>
                <DataGridTextColumn Header="组件名称" Binding="{Binding ComponentName}"/>
                <DataGridTextColumn Header="数量" Binding="{Binding Quantity}"/>
                <DataGridTextColumn Header="单位" Binding="{Binding Unit}"/>
                <DataGridTextColumn Header="单价" Binding="{Binding UnitPrice}"/>
                <DataGridTextColumn Header="小计" Binding="{Binding Amount}"/>
                <DataGridTemplateColumn Header="操作">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="编辑">
                                    <materialDesign:PackIcon Kind="Edit"/>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="删除">
                                    <materialDesign:PackIcon Kind="Delete"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>