---
description: 修改代码前确认是否超出用户指令范围
alwaysApply: true
---

# 代码修改范围控制

## 规则

修改代码前，确认改动是否在用户明确指令范围内：

1. **指令范围内**：直接修改
2. **指令范围外**：先询问用户是否同意修改，再执行

## 判断标准

用户指令明确包含以下情况时，视为指令范围内：
- 指定了具体文件路径或文件名
- 指定了具体功能/模块名称
- 指定了具体要修改的内容

## 示例

**用户说："把水槽类改为水槽"**
- ✅ 修改 ProductCategory.cs 里的"水槽类"→"水槽"（在指令范围内）
- ❓ 修改 DbService.cs 的别名映射（与指令相关但非直接指明，先问）
- ❓ 修改 AddProductMaterialWindow.xaml.cs（不在指令范围内，先问）

**用户说："修复登录bug"**
- ✅ 修改登录相关代码（直接相关）
- ❓ 同时修复无关的样式问题（不在指令范围内，先问）

## 询问格式

```
⚠️ 这个改动涉及 [文件名]，是否同意修改？
- 改动：[简要说明改动内容]
- 原因：[为什么需要改]
```
