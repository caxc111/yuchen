<UserControl x:Class="MaterialImportTool.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid Background="#FFF5E6D3">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="White" Margin="0,0,0,10">
            <Button DockPanel.Dock="Left" Background="Transparent" BorderThickness="0" Padding="10"
                    Click="BackButton_Click">
                <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24" Foreground="#333333"/>
            </Button>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Settings" Width="28" Height="28" Foreground="#FF9800"/>
                <TextBlock Text="系统设置" FontSize="24" Margin="10,0,0,0" Foreground="#333333"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1" Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="数据库路径：" VerticalAlignment="Center"/>
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                <TextBox Width="400" Text="{Binding DbPath}"
                         />
                <Button Content="浏览" Width="80" Margin="10,0,0,0"
                        Command="{Binding BrowseDbPathCommand}"
                        Background="Transparent" BorderThickness="1" BorderBrush="#DDDDDD" Padding="10,5"/>
            </StackPanel>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="编码前缀：" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="1" Width="200" Text="{Binding CodePrefix}"
                     />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="编码长度：" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" Width="200" Text="{Binding CodeLength}"
                     />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="OCR语言：" VerticalAlignment="Center"/>
            <ComboBox Grid.Row="3" Grid.Column="1" Width="200"
                      SelectedItem="{Binding OcrLanguage}"
                      >
                <ComboBoxItem Content="chi_sim"/>
                <ComboBoxItem Content="chi_tra"/>
                <ComboBoxItem Content="eng"/>
                <ComboBoxItem Content="jpn"/>
            </ComboBox>

            <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="重置" Margin="0,0,10,0" Padding="15,5"
                        Command="{Binding ResetSettingsCommand}"
                        Background="Transparent" BorderThickness="1" BorderBrush="#DDDDDD"/>
                <Button Content="保存设置"
                        Command="{Binding SaveSettingsCommand}"
                        Background="#FF9800" Foreground="White" BorderThickness="0" Padding="15,5"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>