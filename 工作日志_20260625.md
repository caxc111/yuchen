# 工作日志 2026-06-25

## 09:10

### Bug修复
- **修复 MaterialSelectorDialog 编译警告**: 修复 QuantityTextBox_TextChanged 方法中 FactoryMaterial.QuantityDecimalPlaces 属性访问的歧义警告
 - 修改文件：MaterialSelectorDialog.xaml.cs
 - 状态：已完成
 - 备注：增强了防御性代码，添加 try-catch 保护

## 09:49

### Bug修复
- **修复产品管理页面编辑后保存不显示问题**: 修复 `UpdateProduct` 方法直接替换 `ObservableCollection` 对象导致 DataGrid 绑定失效的问题
 - 修改文件：ProductManagementViewModel.cs
 - 状态：已完成
 - 备注：将 `Products[index] = product` 替换为直接更新已有对象的各个属性，保持对象引用不变，让 `INotifyPropertyChanged` 能正确通知 UI 刷新

---

## 09:40

### UI优化
- **统一所有弹窗窗口的三件套按钮（关闭/最小化/最大化）**: 为缺失窗口控制按钮的弹窗补充完整功能，并修复图片查看器窗口样式
 - 修改文件：ImageViewerWindow.xaml, ImageViewerWindow.xaml.cs, MaterialTypePickerDialog.xaml, MaterialTypePickerDialog.xaml.cs, ProductManagementDialog.xaml, ProductManagementDialog.xaml.cs, PartManagementDialog.xaml, PartManagementDialog.xaml.cs, CustomPartEditorDialog.xaml, CustomPartEditorDialog.xaml.cs, PartEditorDialog.xaml, PartEditorDialog.xaml.cs, FactoryDialog.xaml, FactoryDialog.xaml.cs
 - 状态：已完成
 - 备注：
   - MaterialTypePickerDialog: 从无窗口按钮 → 添加完整三件套
   - ProductManagementDialog/PartManagementDialog/CustomPartEditorDialog/PartEditorDialog/FactoryDialog: 从仅有关闭按钮 → 添加最小化/最大化按钮
   - ImageViewerWindow: 修复深色背景下按钮不可见问题，改为与其他弹窗一致的样式（圆角边框 + 统一三件套）

---

## 14:52

### 重构
- **将"添加产品"和"编辑产品"对话框拆分为独立窗口**: 分离业务逻辑，添加产品时直接落库，编辑产品时从数据库加载
 - 新增文件：AddProductDialog.xaml, AddProductDialog.xaml.cs, EditProductDialog.xaml, EditProductDialog.xaml.cs
 - 修改文件：ProductManagementView.xaml.cs
 - 删除文件：ProductManagementDialog.xaml, ProductManagementDialog.xaml.cs
 - 状态：已完成
 - 备注：
   - 添加产品：产品编码可编辑、项目代码可编辑、保存时直接落库
   - 编辑产品：产品编码只读、项目代码只读、从数据库加载已有数据、支持删除产品
   - 面积、成本合价、均价字段仅在编辑产品时显示

---

## 15:03

### 功能优化
- **产品编码生成流程改为用户手动输入序号**: 点击生成编码后弹出输入框让用户输入序号，数据库检查重复
 - 修改文件：AddProductDialog.xaml.cs, DbService.cs
 - 状态：已完成
 - 备注：
   - 新增 `CheckProductCodeExists` 方法检查产品编码是否已存在
   - `GenerateProductCodeButton_Click` 改为弹出输入框让用户输入序号
   - 编码格式：`XX-A-1R1B-用户输入`
   - 重复时提示"已存在，请重新输入"
